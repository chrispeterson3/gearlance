<% if notice %>
  <p><%= notice %></p>
<% end %>

<% if @user.id == session[:user_id] %>
  <%= link_to "Edit my profile", edit_user_url(@user.id) %>
  <%= link_to "Delete my profile", user_url(@user.id), method: :delete, confirm: "Sure?" %>
  <h1>
    <%= gravatar_for @user, 250 %><br>
    Welcome back, <%= @user.first_name %>
  </h1>
<% else %>
  <h1><%= @user.first_name %> <%= @user.last_name %></h1>
<% end %>

<ul>
  <li><b>Name:</b> <%= @user.first_name %> <%= @user.last_name %></li>
  <li><b>Email:</b> <%= @user.email %></li>
  <li><b>Avatar Photo:</b> <%= @user.avatar %></li>
  <li><b>Phone:</b> <%= @user.phone %></li>
  <li><b>Address:</b> <%= @user.address_1 %></li>
  <li><b>Address:</b> <%= @user.address_2 %></li>
  <li><b>City:</b> <%= @user.city %></li>
  <li><b>State:</b> <%= @user.state %></li>
  <li><b>Zip:</b> <%= @user.zip %></li>
  <li><b>Country:</b> <%= @user.country %></li>
</ul>


<h3>My Reservations</h3>
  <table>
    <thead>
      <tr>
        <th>Image</th>
        <th>Category</th>
        <th>Name</th>
        <th>Description</th>
        <th></th>
        <th></th>
        <th></th>
        <th></th>
        <th></th>
        <th></th>
      </tr>
    </thead>

    <tbody>
        <tr>
          <td>          </td>
          <td></td>
          <td></td>
          <td></td>
          <td></td>
          <td></td>
          <td></td>
          <td></td>

            <td></td>
            <td></td>
        </tr>
    </tbody>
  </table>

<% if @user.items.count > 0 %>
<h3>My Gear</h3>
  <table>
    <thead>
      <tr>
        <th>Image</th>
        <th>Category</th>
        <th>Name</th>
        <th>Description</th>
        <th>Daily Price</th>
        <th>Weekly Price</th>
        <th>Monthly Price</th>
        <th></th>
        <th></th>
        <th></th>
      </tr>
    </thead>

    <tbody>
      <% @user.items.each do |item| %>
        <tr>
          <td><% if item.image_content_type.present?  %>
            <%= link_to image_tag(item.image.url(:small)), item_url(item.id) %>
          <% else %>
            <%= link_to image_tag(item.photo_url), item_url(item.id) %>
          <% end %>
          </td>
          <td><%= item.category.name %></td>
          <td><%= item.name %></td>
          <td><%= item.description %></td>
          <td>$<%= item.daily_price %></td>
          <td>$<%= item.weekly_price %></td>
          <td>$<%= item.monthly_price %></td>
          <td><%= link_to 'Show', item, class: 'btn btn-primary' %></td>
          <% if @user.id == session[:user_id] %>
            <td><%= link_to 'Edit', edit_item_url(item), class: 'btn btn-primary' %></td>
            <td><%= link_to '86 for life', item, class: 'btn btn-danger', method: 'delete', data: { confirm: "Do you really want to delete this item?" } %></td>
          <% end %>
        </tr>
      <% end %>
    </tbody>
  </table>
<% elsif @user.id == session[:user_id] %>
  <h3>List some gear!</h3>
  <%= link_to "List my gear!", new_item_url, class: "btn" %>
<% else %>

<% end %>


